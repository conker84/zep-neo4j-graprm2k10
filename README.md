# Repo of GraphRM session "Interactive Notebooks with Zeppelin and Neo4J"

Spin-up the stack

```
docker-compose up -d
```

Note that the "Load Dependencies" notebook must be executed just once at the first start of Zeppelin
